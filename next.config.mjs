/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "i.ibb.co",
            "www.motorcyclevalley.com",
            "speedforce.in",
            "images.pexels.com"
        ],
    },
};

export default nextConfig;
